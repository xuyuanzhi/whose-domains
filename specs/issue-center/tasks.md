# 问题中心实施任务

用户已授权审核无误后继续；设计已审核修订，按以下计划实施。

- [x] 1. 数据和采集基础（需求1、2、4）：迁移、配置、脱敏、有限队列、事务聚合、幂等与并发测试。
- [x] 2. 请求与浏览器采集（需求1、2、4）：过滤器、异常入口、限流上报、fetch/XHR和错误监听、模板加载。
- [x] 3. 任务采集（需求1）：域名/DNS、监控、通知任务入口与实际catch覆盖。
- [x] 4. 后台问题中心（需求3）：管理员API、筛选分页、详情、状态/备注/版本、菜单。
- [x] 5. 清理与配置（需求4）：保留期、分批清理、版本环境与启用开关、部署说明。
- [x] 6. 验证（全部需求）：真实MySQL并发、MVC、JS及浏览器流程，相关既有回归测试。

## 审核结论

原设计的“临时客户端问题迁移”会混淆已保留累计次数和已清理明细，也可能搬移管理员已处理的问题。改为服务端请求由服务端计数、浏览器只补观察标记；无响应的网络错误独立计数。客户端上报位置改用服务端发布的资源/路由允许清单，避免任意URL、栈或正文入库。

## 实施与验证结果（2026-09-19）

- 需求、设计已审核；按用户“审核无误后继续”的授权完成开发，无额外阶段确认。
- 已完成四张表迁移、core采集/聚合服务、两端HTTP与浏览器接入、任务接入、管理员API与Layui页面、清理任务及配置。
- 修正了原设计的临时问题迁移策略；客户端只补服务端观察标记。补充解决前发生的延迟事件不重开规则。
- 后台增加 native 代理头支持，避免HTTPS代理场景同源校验误拒绝；导航原有测试更新为包含新问题中心入口。
- 67项相关Java测试通过（含7项真实MySQL数据测试、实际Chrome联调）；12项JS运行时回归通过。
- 真实MySQL测试使用独立localhost:13379实例及wesite_issue_center_test库，未读取或修改业务数据库。
- 浏览器验证：查询500、Loading退出、前后端关联去重、状态/备注/修复版本、新故障重开、旧版本保存冲突、匿名访问拒绝、390px窄屏详情。
- 保留原域名查询500修复，并重跑3项查询接口回归、2项报告页面回归、通知/监控和后台权限/菜单测试。
- 三模块编译通过，git diff --check通过。未提交、推送或部署，未执行生产迁移。
- 部署与启用说明：doc/issue-center.md；采集默认关闭，先迁移再配置环境/版本并启用。

### 代码审查修复验证

- 修复浏览器不同脚本行列位置被合并的问题，保留 Java 方法级指纹稳定性。
- 使用独立 resolved_at 判断复发；补充备注不再移动解决时间，增加已有表升级及回填脚本。
- 异步超时/错误等到完成回调采集最终 HTTP 状态，跨错误分派保持只采集一次。
- 修复后 22 项相关 Java 测试通过（8 项采集、5 项上报、9 项真实 MySQL）；包含旧表升级、重复升级、延迟复发及再次解决的回归验证。
